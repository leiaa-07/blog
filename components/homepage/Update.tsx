import clsx from 'clsx';

const Update = () => {
  const className = clsx(
    'text-2xl font-medium leading-9 tracking-tight text-gray-500 dark:text-gray-400 md:text-lg md:leading-7'
  );

  return (
    <div className={className}>
      test
    </div>
  );
};

export default Update;
